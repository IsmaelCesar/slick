<% pending_invites = j render partial: 'messaging/user_invites/list_pending', locals: { pending_invites: pending_invites } %>
(function (){
  let listing_dashboard = document.querySelector('.messaging-chat-content')
  if(listing_dashboard){
    listing_dashboard.innerHTML = '<%= pending_invites %>'
  }
})()