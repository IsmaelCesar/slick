<% invites_list = j render partial: 'messaging/user_invites/list_received', locals: { received_invites: received_invites } %>
(function (){
  let listing_dashboard = document.querySelector('#listing-dashboard')
  if(listing_dashboard){
    listing_dashboard.innerHTML = '<%= invites_list %>'
  }
})()